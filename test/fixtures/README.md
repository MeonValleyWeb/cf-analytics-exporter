# Synthetic fixtures

These settings are fabricated examples matching the documented Cloudflare settings-node shape. They are not captured from a real zone and do not establish plan entitlements or schema acceptance. Live verification and redacted real-response fixtures are still pending.
